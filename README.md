### about

All configuration is oriented to bash script, definition.nix is an immutable file in that repository, everything that is defined in it by bash script is only for installation, definition.nix it was thought to be portable between different configurations
